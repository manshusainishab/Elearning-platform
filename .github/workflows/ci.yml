name: CI

on:
    push:
        branches:
            - main
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "CI workflow is running!"